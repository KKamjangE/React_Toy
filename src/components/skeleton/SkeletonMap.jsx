import { Skeleton } from "@mui/material";

const SkeletonMap = () => {
    return (
        <Skeleton variant="rectangular" sx={{ width: "100%", height: 650 }} />
    );
};

export default SkeletonMap;
